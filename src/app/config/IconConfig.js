import React from 'react';
import { 
    Assignment,
    FolderOpen,
    Save,
    Add
} from '@material-ui/icons';

export const iconMap = {
    gameConfig: <Assignment />,
    newScene: <Add />,
    openScene: <FolderOpen />,
    saveScene: <Save />
}